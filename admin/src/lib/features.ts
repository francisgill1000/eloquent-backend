// Temporary feature flags.
//
// WHATSAPP_ENABLED gates every user-facing WhatsApp surface in the admin app
// (Chats tab + pages, the setup nudge, the Settings link, the Profile support
// button). It is OFF for now — flip to `true` to bring the whole feature back.
export const WHATSAPP_ENABLED: boolean = false;
